{
  "name": "edge-custom-cms",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "lint": "prettier --check src/ wrangler.jsonc",
    "lint:fix": "prettier --write src/ wrangler.jsonc",
    "db:migrate:local": "wrangler d1 execute cms_db --file=/dev/stdin --local",
    "db:migrate:remote": "wrangler d1 execute cms_db --file=/dev/stdin --remote"
  },
  "dependencies": {
    "hono": "^4.12.29",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260712.1",
    "prettier": "^3.6.2",
    "typescript": "^7.0.2",
    "wrangler": "^4.110.0"
  }
}
