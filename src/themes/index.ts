// Theme discovery hub
// Each theme is a .ts file that calls registerTheme() on import.
// Add new themes here — order doesn't matter.

import './default.js';

// ── How to create a theme ─────────────────────────────────────────
// 1. Copy src/themes/default.ts to src/themes/my-theme.ts
// 2. Change the id, name, css, and optionally override templates
// 3. Add: import './my-theme.js';  ← here in this file
// 4. Commit, push, deploy
// 5. Select it in Admin → Settings → Public Site Theme
//
// See src/themes/example.ts for a complete walkthrough.
